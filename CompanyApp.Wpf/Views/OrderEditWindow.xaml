<Window x:Class="CompanyApp.Wpf.Views.OrderEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}"
        SizeToContent="WidthAndHeight"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        MinWidth="460">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Дата:" />
        <DatePicker Grid.Row="0" Grid.Column="1" Margin="0,0,0,8"
                    SelectedDate="{Binding OrderDate}"
                    IsEnabled="{Binding CanSave}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Сумма:" />
        <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"
                 Text="{Binding Amount, UpdateSourceTrigger=PropertyChanged}"
                 IsReadOnly="{Binding IsReadOnly}" />

        <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Сотрудник:" />
        <ComboBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding Employees}"
                  SelectedItem="{Binding SelectedEmployee}"
                  DisplayMemberPath="FullName"
                  IsEnabled="{Binding CanSave}" />

        <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,8,8" VerticalAlignment="Center" Text="Контрагент:" />
        <ComboBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"
                  ItemsSource="{Binding Counterparties}"
                  SelectedItem="{Binding SelectedCounterparty}"
                  DisplayMemberPath="Name"
                  IsEnabled="{Binding CanSave}" />

        <StackPanel Grid.Row="4" Grid.Column="0" Grid.ColumnSpan="2"
                    Margin="0,12,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
            <Button Width="100" Margin="0,0,8,0" Content="Сохранить"
                    Command="{Binding SaveCommand}"
                    Visibility="{Binding CanSave, Converter={StaticResource BooleanToVisibilityConverter}}" />
            <Button Width="100" Content="Закрыть" Command="{Binding CancelCommand}" />
        </StackPanel>
    </Grid>
</Window>
